<% flash.each do |flash_type, msg| %>
  <div class="alert alert-<%= flash_type == 'notice' ? 'success' : 'danger' %>" role="alert">
    <a href="#" class="close" data-dismiss="alert">×</a>
    <%= msg %>
  </div>
<% end %>
